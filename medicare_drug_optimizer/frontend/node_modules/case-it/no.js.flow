/* @flow strict */

/**
 * Remove any starting case from a `string`, like camel or snake, but keep
 * spaces and punctuation that may be important otherwise.
 */
declare export function noCaseIt(str: string): string